name: sim

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Download Tools
        run: |
          sudo apt-get install python3-dev
          pip3 install -r scripts/requirements.txt
          wget -O - https://raw.githubusercontent.com/sifferman/hdl-tool-installer/main/install | bash -s -- . --oss-cad-suite --zachjs-sv2v
          echo "$GITHUB_WORKSPACE/oss-cad-suite/bin" >> $GITHUB_PATH
          echo "$GITHUB_WORKSPACE/zachjs-sv2v" >> $GITHUB_PATH

    #   - run: make lint

      - run: make sim DUT=evt2_decoder
      - run: make sim DUT=input_fifo

    #   - run: make sim DUT=uart_debug # unnecessary test
    #   - run: make sim DUT=uart_rx    # unnecessary test
    #   - run: make sim DUT=uart_tx    # unnecessary test

    #   - run: make sim DUT=gradient_map_core # still broken
    #   - run: make sim DUT=gradient_map_top # still broken
    #   - run: make sim DUT=gradient_gesture_classifier # takes a while to run
    #   - run: make sim DUT=gradient_mapping
    #   - run: make sim DUT=gradient_systolic_array
    #   - run: make sim DUT=gradient_weight_ram

    #   - run: make sim DUT=voxel_bin_core # still broken
    #   - run: make sim DUT=voxel_bin_top # still broken
    #   - run: make sim DUT=voxel_binning # takes a while to run
    #   - run: make sim DUT=voxel_gesture_classifier
    #   - run: make sim DUT=voxel_systolic_array
    #   - run: make sim DUT=voxel_weight_ram

    # - run: make sim DUT=MatMul

      - run: make clean